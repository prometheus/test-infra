go:
    # Whenever the Go version is updated here, .travis.yml and
    # .circle/config.yml should also be updated if in use.
    version: 1.12
repository:
    path: github.com/prometheus/prombench
build:
    binaries:
        - name: prombench
          path: ./cmd/prombench
        - name: ./cmd/scaler/scaler
          path: ./cmd/scaler
        - name: ./cmd/fake-webserver/fake-webserver
          path: ./cmd/fake-webserver
    flags: -a
