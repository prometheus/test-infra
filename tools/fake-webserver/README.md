# fake-webserver

It's a Fake Webserver to test load against the release and the Pull Request.

## Building Docker Image

To build the Docker image for `fake-webserver`:

```
docker build -t prominfra/fake-webserver:master .
```
