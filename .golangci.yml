issues:
  max-same-issues: 0

linters:
  enable:
    - errorlint
    - gofumpt
    - goimports
    - gomodguard

linters-settings:
  goimports:
    local-prefixes: github.com/prometheus/test-infra
  gomodguard:
    blocked:
      modules:
        - github.com/pkg/errors:
            recommendations:
              - errors
              - fmt

output:
  sort-results: true

run:
  skip-files:
    # Skip autogenerated files.
    - ^.*\.(pb|y)\.go$
  timeout: 5m
